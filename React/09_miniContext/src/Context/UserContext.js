import React from "react";

const UserContext = React.createContext(); // This is used to create a context and it will return an object with two properties Provider and Consumer

export default UserContext;